import type { Meta, StoryObj } from '@storybook/react';
import { <%= _.capitalize(name)%> } from './<%= name%>';

const meta: Meta<typeof <%= _.capitalize(name)%>> = {
  component: <%= _.capitalize(name)%>,
  title: '<%= name%>',
};
export default meta;
type Story = StoryObj<typeof <%= _.capitalize(name)%>>;

export const Primary:Story = {
  args: {},
};
